# MVP : fetch.py et build_repo.py n'utilisent QUE la bibliotheque standard
# Python (>=3.10) + Docker et dpkg-dev cote hote. Rien a installer ici.
#
# Pour l'etape suivante (API HTTP + file de jobs), decommentez :
# fastapi>=0.110
# uvicorn[standard]>=0.29
# redis>=5.0
# rq>=1.16
# pydantic>=2.6
